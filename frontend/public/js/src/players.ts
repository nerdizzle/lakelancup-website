import { h, Component, render } from '../contrib/node_modules/preact/src/index';

export default class Players extends Component {
    render(props, state) {
        return (
          h("div", {id:"app"},
          h("div", {class: "lakelandcup-container"},
           
              
            
            )
          )
        );
    }
}
//render(h(Players,null), document.body);